#!/usr/local/env sh

case "$1" in
    --compile)
        ;;
    --jit)
        ;;
    --help)
        ;;
    --repl)
        ;;
    -V|--version)
        echo "FluidScripten MiniCompiler v0.0.1-alpha.1"
        exit 1
        ;;